# 03 代码审核报告

> 审核日期：2026-09（通篇整理任务）；方法：6 个分层并行审核代理精读 + 依赖图实测 +
> 问题逐条实证（可复现才入表）；范围：src 全部 37 个 .ts（不含 extract/*.mjs 内部算法与
> stencilData 生成资产）+ scripts + 资源管线约定；opcpkg 层另与 C++ 参考仓逐文件逐分支比对。
> 分级：P0 = 明确逻辑错误/线上失败；P1 = 边界健壮性/契约不符/结构反向依赖；
> P2/P3 = 风格/过时注释/改进面。
> 状态图例：✅ 已修复（本任务内）；⏳ 遗留台账（见 §八）；✳ 有意保留（文档化）。

## 一、审核结论概览

| 层 | 文件 | P0 | P1 | P2/P3 | 层评价 |
| --- | --- | --- | --- | --- | --- |
| 基础层 core+xml | 8 | 0 | 4 ✅ | 13（✅8 ✳2 ⏳3） | 结构清晰、依赖零违规；解析器宽容度已统一并文档化 |
| 容器层 opcpkg | 6 | 0 | 1 ✅ | 7（✅2 ⏳5） | 高质量 C++ 1:1 平移；主链路无风险 |
| 快照/IR 层 | 4+14mjs | 0 | 1 ⏳ | 5 ⏳ | 三层职责分明、契约逐字段一致；生命周期健壮性待补 |
| 模型/转换/序列化 | 8 | 0 | 0 | 14（✅6 ⏳8） | 流水线清晰、环安全；改进面见台账 |
| 渲染层 | 8 | 0 | 0 | 10（✅5 ⏳5） | 双路径分叉点单一、格式扫描全绿；sequence 行命名差 1 与复制簇入台账 |
| gantt/母版/编排 | 9+scripts | 0 | 0 | 10（✅4 ⏳6） | 双路径契约半闭合；抽审项全部确认 |

全仓无 P0。已修 P1 共 7 项（xmlNode×4、zipArchive×1、cli×1、snapshot×1）。

## 二、结构级处置（本任务完成，独立于分层表）

- ✅ **文件归位**：`coordinateTransform` translate→render、`validator` serialize→docmodel
  —— 消除 `render→translate` 与 `render→serialize` 两条反向依赖；纯 import 变更，
  行为零变化（typecheck + 186 测试全绿），并补 `scripts/check-architecture.mjs` 分层
  守门（`npm run check:arch`：37 文件 129 跨组边白名单矩阵，违规 exit 1）。
- ✅ **CLI 退出缺陷**（真实线上缺陷）：单文件/--dir 转换后从不 shutdown → chromium
  子进程句柄悬挂、进程永不退出（实证：转换 2s 完成但进程挂 60s+，serve/批量收尾均
  正常故此前未暴露）；修复 = 两路径 finally `shutdownQuietly()`，2s 内干净退出。
- ✳ **docmodel/model↔documentCore 互引**：双向 `import type`，编译期擦除、运行时零边，
  保留；两侧头注释固化"禁值导入"约束防退化（P2-1）。

## 三、基础层（core ×5 + xml ×3）—— 无 P0

层评价：core 零依赖纯类型层、xml 自包含；导出面克制（两处冗余已清理）；错误消息统一
`[xml]` 前缀。隐患集中在解析器宽容度不均（P1×4 已全部修复并锁测试）。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| X-1 | P1 | xmlNode 根闭合分支 | 成对根闭合即 break，尾随第二根/文本静默截断；多根守卫口径自相矛盾 | ✅ 根闭合后扫至 EOF；根后非空白文本/CDATA 抛错；注释/PI/空白合法 |
| X-2 | P1 | xmlNode parseAttribute | 重复属性静默接受并序列化出非法 XML | ✅ push 前查重抛 `[xml] duplicate attribute` |
| X-3 | P1 | xmlNode decodeEntities | 数字字符引用未按 XML Char 白名单（NUL/代理/FFFE 漏过） | ✅ 按 XML 1.0 Char 生产式校验 |
| X-4 | P1 | xmlNode CDATA/appendText | 行尾归一不覆盖 CDATA 与构造路径，破坏 round-trip 字节不变量 | ✅ 归一收敛到 appendText 入口，树内永不驻留 CR |
| X-5 | P2 | xmlNode parseName | 元素/属性名文法过宽（`<1abc/>` 原样序列化） | ✳ 宽松读取已写入 parseDocument docstring |
| X-6 | P2 | xmlNode decodeEntities | 未定义实体/裸 & 静默保留（首轮往返文本变化） | ✳ 同上（读取宽容，docstring 明示） |
| X-7 | P2 | xmlBuilder 头注释 | "number() 17 位 max_digits10"与最短往返实现不符 | ✅ 头注释改述"最短往返，数值等价" |
| X-8 | P2 | xmlBuilder formatNumberStrict | %g 修剪只覆盖定点形态，docstring 过宽 | ✅ docstring 注明适用范围 |
| X-9 | P2 | xmlBuilder parseNumber | docstring"stod+全消费"与实际 Number() 语义不符 | ✅ docstring 改述实际语义 |
| X-10 | P2 | xmlBuilder setCellAttribute | 纯转发死导出（零调用） | ✅ 删除 |
| X-11 | P2 | core/vsdx 头注释 | "运行时断言整数"无实现支撑 | ✅ 删除该承诺句 |
| X-12 | P2 | core/errors / application | core 版 isMmdError 零调用、application 重复实现 | ✅ 删除 application 本地实现，统一 core 版 |
| X-13 | P2 | xmlNode 解析 | 无嵌套深度上限（极深嵌套 → serialize 栈溢出） | ✅ 65536 深度护栏 |

回归：testxml 新增 5 用例锁定 X-1..X-4/X-13（190/190 全绿）。

## 四、容器层（opcpkg ×6）—— 无 P0

层评价：与 C++ 参考（ziparchive.cpp 711 行等）逐文件逐分支比对，EOCD 反扫/central-local
一致性/data descriptor 双形态/CRC/ZIP64 拒收次序/写路径字节布局/ZipLimits 次序/rId
最小空闲/CT 序列化形态全部吻合；7 码 ZipError 全在途；依赖方向零违规。主链路无风险。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| Z-1 | P1 | zipArchive inflateRaw | inflateRawSync 无 maxOutputLength——声明长度与实际流不符的"错配炸弹"先膨胀超限内存（压缩比 ~10³ 可达数十 GiB），ZipLimits 对真实分配失效，恶意 .vsdx 可 OOM | ✅ `maxOutputLength: usize+1`，对齐 C++ 固定缓冲语义（超量 Z_BUF_ERROR→InvalidArchive） |
| Z-2 | P2 | zipArchive central decode | UTF-8 标志未置 + 非法 UTF-8 字节时 TS 先 fatal decode 抛 UnsafeEntryName；C++ 抛 unsupported legacy | ✅ decodeEntryName 镜像 C++ 分支序（central/local 共用） |
| Z-3 | P2 | zipArchive/package.json | engines ">=22" 而 node:zlib.crc32 需 22.2.0 | ✅ engines 收严 ">=22.2.0"（见注 1） |
| Z-4 | P2 | contentTypes | parse→serialize 恒 Default 在前；C++ 以 DOM 保留源序（交错输入字节序不同） | ⏳ 台账：注释写明前提（自产/常规文件），复刻 DOM 序留待需要 |
| Z-5 | P2 | contentTypes/relationships | 无 OPC 命名空间校验（TS 按元素名、C++ 强制 ns） | ⏳ 台账：双向分歧（无 ns 被补写 vs 带前缀被拒），按 xmlns URI 判定留待 |
| Z-6 | P2 | contentTypes/relationships | Buffer.toString('utf8') 非致命解码（U+FFFD 污染值随 save 写回） | ⏳ 台账：fatal TextDecoder 化 |
| Z-7 | P2 | contentTypes 头注释 | 宣称"解析错误用 Error"但文法错误走 TypeError（与 C++ invalid_argument 忠实对齐） | ✅ 头注释注明口径（见注 2） |

注 1：Z-3 与 Z-7 为文档/声明修正，代码未动；注 2：TypeError 路径即 C++ invalid_argument 平移，非缺陷。

## 五、快照/IR 层（snapshot + mmdtransform）—— 无 P0

层评价：三层职责分明（renderer=浏览器资源层不认识 IR；jsonToDiagram=纯函数仅依赖 core；
translator=组合门面）；队列串行真实（catch 续链不毒化）；evaluate 回调全部合规
（仅 globalThis+形参）；extract 接入 14/14 无遗漏；**契约核对以 16 fixture 逐图类
全字段实测一致**（顶层键/回退默认/可选字段方向/NaN→null 防线均对齐）。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| S-1 | P1 | renderer recreatePage | 只重建页面不重建浏览器：chromium 整体崩溃后 browser_ 永久不可用，serve 模式每次渲染"失败→重建→再失败"，与"崩溃自动重启"注释不符 | ✅ recreatePage 检测 `isConnected()` 失败即弃死浏览器，下次 ensurePage 重 launch |
| S-2 | P2 | renderer shutdown | shutdown 后已入队任务照跑并经 ensurePage 静默重开 chromium 无人关闭 | ✅ ensurePage/入队任务 closed_ 守卫，关后即抛不再重开 |
| S-3 | P2 | renderer evaluate | 无渲染超时：页面挂起型故障永久卡死串行队列 | ⏳ 台账：Promise.race 硬超时 |
| S-4 | P3 | renderer bundle 拼接 | replace(/^export\s+/gm) 对未来的 export default 会产出坏 bundle；"依赖序勿改"注释失实 | ✅ 拼接契约断言（禁 export default/import）+ 注释修正 |
| S-5 | P3 | translator/launch | chromium 未装报错包成 MermaidError 且白费一次重试 | ⏳ 台账：launch 失败单独归类 |
| S-6 | P3 | types.ts 头注释 | "映射层因此消失"与实际保留三张查表不符 | ⏳ 台账：注释修正 |

## 六、模型/转换/序列化层（docmodel/translate/serialize）—— 无 P0/P1

层评价：四步流水职责清晰（门面→importer 三步→xmlPartsBuilder）；DocumentCore 数据+树引用+
脏标记无副作用；coordinateTransform/validator 语义逐条核对成立（与 testcoordinate 断言
一致）；masterClient 默认与切换无残留；pageShapes 守卫为断言语义。弱点：addPage 双序列化
与脏标记存活约定、层内错误文案三制并存。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| M-1 | P2 | model/documentCore 环 | 双向 type-only 环可保留但无防退化机制 | ✅ 两文件头注释固化"禁值导入" |
| M-2 | P2 | model.ts | PageId/ShapeId 三处同名别名源 | ✅ 统一从 core/vsdx.js 引入再导出 |
| M-3 | P2 | diagramImporter | assembleDocumentCore 的 diagram 参数从未使用（void 压制） | ✅ 删参数与调用点实参 |
| M-4 | P2 | xmlPartsBuilder/importer | 页面部件 addPage 空壳入包 + build 全量回刷 = 每页两遍序列化；脏标记存活全局约定 | ⏳ 台账：flush 对缺失部件先 addPart 使 builder 成唯一写入点 |
| M-5 | P2 | renderer refreshConnectors | 导出无调用方（潜伏），且树写入不标脏 | ✅ 删除（渲染层 R-4 同项） |
| M-6 | P2 | model.ts shapeExists | 死代码；与 logicalIdExists 命名近似的"后代"对 | ✅ 删除 shapeExists；logicalIdExists 下沉 docmodel/model.ts |
| M-7 | P2 | 层内错误文案 | output scale vs outputScale 双文案；[phase]/括号/裸前缀三制并存 | ⏳ 台账：统一口径并标注 C++ 原文保留项 |
| M-8 | P2 | testtranslate 头注释 | 仍称"M2 未接入母版打包"与同文件真实母版断言自相矛盾 | ✅ 头注释改述双路径现状 |
| M-9 | P3 | importer resolveType | diagramType 无运行时白名单（联合类型外值静默 Basic 化） | ⏳ 台账 |
| M-10 | P3 | importer 分派 | 空内容专用图静默落通用空页路径 | ⏳ 台账：契约注释写明 |
| M-11 | P3 | importer gantt 分支 | gantt 不用 transform 但全图坐标校验先行（无关 NaN 误抛） | ⏳ 台账：核对 C++ 顺序后决定 |
| M-12 | P3 | model PageModel | shapes/connectors 为 managed-only 不变量未文档化 | ✅ 注册契约注释写入 PageModel |
| M-13 | P3 | masterClient 头注释 | "M2 暂以…M5 接入后"进行时措辞 | ⏳ 台账（masters 文件统一改） |
| M-14 | P3 | documentCore 头注释 | "不需要增量序列化"与 dirtyPages 选择性回刷表述相左 | ✅ 头注释改述现状 |

## 七、渲染层（vsdxdoc/render ×8）—— 无 P0/P1

层评价：renderer.ts"模块函数+工厂派发"忠实 C++；masterless/真实母版双路径每处渲染点
单一 `masterIdFor()!==0` 分叉、无散落分支；cell 写入顺序=协议由 M5 金标准闸门背书
（16 样本逐部件结构对比）；pageShapes 容器守卫被全部专用渲染器使用。数值格式化扫描
未见裸拼 V 值。可拆簇：renderClassShape/renderERShape/renderFlowchartShape/
renderConnectorImpl 独立成文件即可把 renderer.ts 压到 ~100 行编排层（改进面，台账 R-8）。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| R-1 | P2 | sequenceRenderer | lifeline 连接行命名 `Connections.X`+i 用 0 基（X0..X38），Visio 规范 Xn↔IX=n-1（归档 example vsdx 实证 X3↔ToPart102）；idx0 产出不存在行名；ToPart 100/102 亦差 1 | ⏳ 台账：与 C++ 基线/golden 同源（门禁锁死），需三端协同或真机 Visio 开卷确认动态粘附吸收误差后显式接受 |
| R-2 | P2 | renderer renderERShape | ER 空名提前 return 与注释"回退普通路径"不一致（产物空 <Shape/>；mermaid 不可达、API 可达） | ✅ 注释定案（与 C++ return 同款；分派按 diagramType 进本函数，行为不偏离基线） |
| R-3 | P2 | docmodel model | masterId/managed 字段只写不读（契约漂移，与 M-6 同批） | ✅ 删除字段与全部赋值点/断言 |
| R-4 | P2 | renderer refreshConnectors | 死 API（src+tests 零调用；只重渲连接线不处理模型副作用） | ✅ 删除 |
| R-5 | P2 | renderer 尺寸回写 | ER 只回写 h 不回写 w、类图完全不回写（masterless 静态路径端口点与真实框不一致，M5 靠 Visio recalc 掩盖） | ✅ 代码注明"与 C++ 同款、基线已验证"，防误"修复" |
| R-6 | P3 | renderer 775 | 渲染层唯一裸拼数值 `GUARD(String(estW))` | ✅ 统一 number()；估宽改按 UTF-8 字节/3 精确折算（与 C++ 逐字节一致） |
| R-7 | P3 | sequenceRenderer | addActivation 形参 actorId 从未使用 | ✅ 删形参与调用点 |
| R-8 | P3 | git/quadrant/pi/sequence | 2-D 折线/文本标签/newShape 跨文件同构复制（各图估宽档位勿合并） | ⏳ 台账：抽 render/shared 参数化，保留各图独立档位 |
| R-9 | P3 | renderer 763 | 连接线估宽"cjk/3 等效"注释过强（2/4 字节码点偏差） | ✅ 随 R-6 逐字节折算并改注释 |

## 八、gantt/母版/编排层 —— 无 P0/P1

层评价：ganttRenderer 只依赖 docmodel+MasterClient 抽象（不触 masterLibrary 具体类）；
母版双实现装配期单点注入、渲染层不可见；编排层薄而均匀、错误统一 [phase]、退出码
基本正确（一处已修）；抽审复核项全部确认（UTC 日期、周末块周六起、首刻度无 Offset、
UniqueID 顶层限定、pack 空洞 +3 无 off-by-one、relId 保留、mergeStyles 幂等、求值器
分支、shutdown 幂等、并发无死锁）。弱点：masterless 分支在 gantt 无本地几何降级
（契约半闭合）；serve 队列无背压（已修）。

| # | 级别 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- | --- |
| G-1 | P2 | ganttRenderer 818 | 链接 Begin 锚点用原始 startSerial 而条体用解析后 startSerials（多级 after 缓存错位；F 公式在 Visio 重算后纠正） | ⏳ 台账：与 C++ 对齐后改用 startSerials[j] + 补多级 after fixture |
| G-2 | P2 | application convertText | ok=true 先于打包；打包失败 HTTP 回 200 ok 无 vsdx | ✅ ok 在打包成功后置位 |
| G-3 | P2 | application serve | 请求体超 1MB 裸 destroy 无应答；无 Content-Length 预检 | ✅ 回 413 JSON 再断开 |
| G-4 | P2 | application serve | 串行队列无上限/背压（慢请求无界积压） | ✅ 排队上限 32，超出 503 |
| G-5 | P2 | masterLibrary 970 | masterChildShapeIds 跨全部模具按 NameU 合并（同名 Group 子 ID 混入未打包模具） | ⏳ 台账：收窄到本次 pack selection |
| G-6 | P2 | masterLibrary 374 | 单位字面量（18MM）数值前缀截断求值成功，写错 V 而非软失败 | ⏳ 台账：单位后缀软失败或换算（逐式核对 C++） |
| G-7 | P2 | ganttRenderer masterless | masterless 下 gantt 无本地几何降级，任务条仍写悬空 MasterShape 骨架 | ⏳ 台账：明确"不支持"并在入口拒绝 或 实现降级 |
| G-8 | P2 | cli --serve | 内联端口无校验（--port 有），非法值 exit 1 而非 usage exit 2 | ✅ parsePort 统一校验（exit 2） |
| G-9 | P2 | gen-stencils/生成头 | "npm files 白名单 + prepare-stencils"注释描述的机制不存在（实际仅 private:true 兜底） | ✅ 注释改述现状红线 |
| G-10 | P2 | goldenCompare | GUID 按"同为 36 位"宽松等价，注册链接错无法被 gate 捕获 | ⏳ 台账：产物侧 GUID 一致性自检 |

## 九、遗留问题台账（⏳ 汇总）

| # | 层 | 问题摘要 | 建议路径 |
| --- | --- | --- | --- |
| S-3/S-5/S-6 | 快照 | 渲染超时缺失；launch 错误归类；types.ts 注释失实 | Promise.race 硬超时（含超大输入分级）；launch 失败单独归类并提示 npx playwright install；注释修正 |
| M-4/M-7/M-9..M-11/M-13 | 模型 | 双序列化；文案统一；白名单闸口；空图语义；gantt 校验序；masterClient 注释 | 逐项小步重构，先补用例 |
| R-1/R-8 | 渲染 | sequence 行命名差 1；复制粘贴簇 | R-1 需真机 Visio 开卷确认（golden 同源）；R-8 抽 shared 保留档位 |
| Z-4/Z-5/Z-6 | 容器 | CT 源序；命名空间校验；UTF-8 致命解码 | 需要 open→save 字节对齐承诺时处理 |
| G-1/G-5/G-6/G-7/G-10 | gantt/母版 | 链接锚点；childIds 范围；单位字面量；masterless 降级；GUID 自检 | 先核对 C++ 对应行为再动，配套 fixture |
| X-5/X-6 | 基础 | 坏名/未知实体宽松读取 | ✳ 有意保留（docstring 已明示半严格口径） |

## 十、守门与回归（本任务落地）

- `npm run check:arch`：分层白名单矩阵守门（新增 `scripts/check-architecture.mjs`），
  越层/反向依赖即 exit 1——结构红线可执行化；
- 最终门禁已执行：`tsc --noEmit` ✓ + `vitest run` 191 用例 ✓ + `check:arch` ✓ +
  `npm run build` ✓（golden 16 样本结构等价在 191 用例内）；
- 提交归档：修复 4 笔（xml/cli+serve/zip/snapshot）+ 结构整理 1 笔 + 文档 1 笔，
  每笔经规范提交信息；历史错位已用 rebase/filter-branch 修正并清理原引用。
